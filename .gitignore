README

target
/updater
.env
*.key
*.key.pub

# Node
node_modules/

# Test coverage
coverage/
teralaunch/coverage/
.worktrees/
.DS_Store
.omc

# Game binaries (not needed for launcher build)
teralaunch/src-tauri/src/TERA/Binaries/
*.dll

# Claude Code per-session scratch state
.claude/

# Visual Studio IDE scratch dir (contains DPAPI-encrypted per-machine
# IIS Express cert bindings, content-index caches, solution state — not source).
.vs/

# Playwright HTML reports (generated locally; contents are binary screenshots)
teralaunch/playwright-report/
